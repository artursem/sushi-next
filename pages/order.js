import { menu } from '../components/sushi/items';
import Menu from '../components/sushi/Menu';

const Order = () => {
	return (
		<main>
			<Menu menu={menu} />
		</main>
	);
};

export default Order;
