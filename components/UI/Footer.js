import classes from './Footer.module.css';

const Footer = () => {
	return (
		<footer className={classes.footer}>
			<p>footer</p>
			<p>footer</p>
			<p>footer</p>
		</footer>
	);
};

export default Footer;
